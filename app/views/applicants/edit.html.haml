- model_class = Applicant
- if admin?
  .page-header
    %h1=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => model_class.model_name.human.titleize
  = render :partial => "form"
- else
  %h3
    Voit päivittää puhelinnumerosi, mikäli se ei ole oikein.
    Puhelinnumeroa käytetään, vain mikäli tulee kiireistä viestintää, kuten odottamattomat muutokset haastatteluaikan.
  = form_for @applicant, :html => { :class => 'form-horizontal' } do |f|
    .control-group
      = f.label :phone_number, :class => 'control-label'
      .controls
        = f.telephone_field :phone_number, :class => 'text_field'
    .form-actions
      = f.submit nil, :class => 'btn btn-primary'
      = link_to t('.cancel', :default => t("helpers.links.cancel")), applicants_path, :class => 'btn'
